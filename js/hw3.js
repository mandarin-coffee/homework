export function sumIntegerNumbers (x = 50) {
    for (let i = 50; i <= 100; i++) {
        //console.log( i + i );
    }

    return true;
}

export function multiplicationTable (x = 7) {
    for (let i = 0; i <= 10; i++) {
        //console.log(`${x} * ${i} = ${x * i}`);
    }

    return true;
}
